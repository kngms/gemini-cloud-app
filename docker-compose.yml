version: '3.8'

services:
  gemini-app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - MODEL_ID=${MODEL_ID:-gemini-2.0-flash}
      - PORT=8080
    env_file:
      - .env
    restart: unless-stopped
